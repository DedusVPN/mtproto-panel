# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Локальные секреты и заметки (не в репозиторий)
.env
double-hop-xray.txt
instr.txt

# Старые локальные JSON (до PostgreSQL) и временные файлы
data/servers.json
data/metrics_history.json
data/monitor_settings.json
data/*.json.tmp

# Docker / панель
secrets/

# Frontend build artifacts (генерируются npm run build / Docker)
static/
frontend/node_modules/
frontend/dist/
