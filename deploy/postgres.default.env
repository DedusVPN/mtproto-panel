# Дефолты для Docker Compose (перекрываются вашим .env при совпадении имён ключей).
# Порядок в compose: сначала этот файл, затем .env — значения из .env имеют приоритет.
POSTGRES_USER=panel
POSTGRES_PASSWORD=panel
POSTGRES_DB=panel
